<% provide(:title, "Text Messages") %>
<% provide(:description, "SMS messages from people.") %>

<h1>Text Messages</h1>

<% @text_messages.each do |text_message| %>
  <%= render 'text_message', :text_message => text_message %>
<% end %>

<%= paginate @text_messages %>
